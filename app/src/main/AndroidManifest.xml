<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentbag">
    <!-- 🔥 الصلاحيات -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="StudentBag"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.StudentBag">
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SummaryActivity"
            android:exported="true" />
        <activity
            android:name=".activities.EvaluationActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SubjectDetailsActivity"
            android:exported="false" /> <!-- الشاشة الرئيسية -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- شاشة المواد -->
        <activity
            android:name=".activities.SubjectsActivity"
            android:exported="false" /> <!-- شاشة إضافة مهمة -->
        <activity
            android:name=".activities.TasksActivity"
            android:exported="false" /> <!-- شاشة عرض المهام -->
        <activity
            android:name=".activities.TasksListActivity"
            android:exported="false" /> <!-- شاشة التخطيط الذكي -->
        <activity
            android:name=".activities.SmartPlanActivity"
            android:exported="false" />

        <receiver android:name=".utils.NotificationReceiver" />

        <activity
            android:name=".activities.PaymentSuccessActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="studentbag"
                    android:host="success" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>

        </provider>
        <activity
            android:name=".activities.SettingsActivity" />

        <activity
            android:name=".activities.PrivacyPolicyActivity" />
    </application>

</manifest>